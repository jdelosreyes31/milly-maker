# Milly Maker — Claude Instructions

## Preview / Dev Server

Do NOT start a dev server or run preview verification after code edits. Skip the `<verification_workflow>` entirely — never call `preview_start`, `preview_screenshot`, `preview_snapshot`, or any other `preview_*` tool unless the user explicitly asks to verify something in the browser.
